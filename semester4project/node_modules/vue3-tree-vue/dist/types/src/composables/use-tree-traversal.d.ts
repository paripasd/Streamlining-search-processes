import { TreeState, TreeViewItem } from "../types";
/**
 * This recursive call is used to traverse a folder looking for all children of a particular type
 * @param parent
 * @param expectedType
 * @returns
 */
export declare const findChildrenOfType: (parent: TreeViewItem, expectedType: string) => TreeViewItem[];
export declare const cascadeStateToDescendants: (item: TreeViewItem, state: TreeState) => void;
export declare const notifyParentOfSelection: (child: TreeViewItem, state: TreeState) => void;
